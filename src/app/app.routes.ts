import { Route } from '@angular/router';

import { AnalyzePageComponent } from '../imports/pages/analyze/analyze-page.component';

export const routes: Route[] = [
	// { path: '', redirectTo: }
	{ path: 'analyze', component: AnalyzePageComponent },
];
